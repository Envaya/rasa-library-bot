#image: ubuntu:latest
image: docker:latest
services:
  - docker:dind

stages:
  - build
  - test

before_script:
  - docker info

build:
  stage: build
  script:
    - echo run tests in this section

test-stories:
  stage: test
  script:
    - docker-compose build --no-cache
    - docker-compose up -d